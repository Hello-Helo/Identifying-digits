# Correspondente a parimeira tarefa do EP de Computação III

import NumPy as np
import Matplotlib as mp

def rot(W, n, m, i, j, c, s)

# Resolver Wx = b usando uma transformação tal que W_{nxm} vira R, uma
# matriz triangular superior

W = np.matrix()
